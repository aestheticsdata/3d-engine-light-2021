/// <reference types="vite/client" />

// Replaced at build time by the `define` in vite.config.js, which reads the
// version out of package.json. Consumed by src/ui/buildInfo.ts.
declare const __APP_VERSION__: string;
