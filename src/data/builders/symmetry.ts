// The two tables five solids in this folder enumerate over.
//
// They were written out in five separate files, which is five chances for one
// of them to be spelled [1, -1, 0] and produce a solid that still renders. Inert
// data with no behaviour, so they stay plain consts rather than becoming a class
// (decisions.md D1a).

export const AXES = [0, 1, 2];

export const SIGNS = [1, -1];
